@Library('shlib1')_
pipeline
{
    agent any
    tools{
    maven 'Maven'
    }

    
   stages{
    stage('Build')
        {
            steps{
              sh 'mvn clean install'
                 }
         }
         
         stage('Nexus')
    {
     steps{
          
            sh   'curl  admin:admin123 -H "Accept: application/json" "http://3.15.18.214:8081/nexus/service/local/repositories/repo/status" '
         
}
}
}
}
