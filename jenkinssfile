@Library('shlib1')_
pipeline
{
    agent any
    tools{
    maven 'Maven'
    }

    
   stages{
   /* stage('Build')
        {
            steps{
              sh 'mvn clean install'
                 }
         }*/
         
         stage('Nexus')
    {
     steps{
          
         repo(JSON)
}
}
}
}
